[package]
name = "karaconf"
version = "0.1.0"
edition = "2021"
description = "Manage Karabiner-Elements profiles from TOML"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
dirs = "5"
